Needs support for lisp packages, with `builtin` being the default package imported automatically

Needs functions
- exit
- ls
- defun
- defmacro
- cd
- if
- cond

needs documentation with `show` function that works with packages, functions, variables
